{
  "name": "delegate",
  "description": "Event delegation component",
  "version": "0.2.3",
  "repository": "zenorocha/delegate",
  "keywords": [
    "event",
    "events",
    "delegate",
    "delegation"
  ],
  "dependencies": {
    "closest": "0.0.1",
    "component-event": "0.1.4"
  },
  "devDependencies": {
    "browserify": "^11.2.0"
  },
  "scripts": {
    "build": "browserify src/delegate.js -o dist/delegate.js"
  }
}
